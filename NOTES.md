Cumbersome: 
In the setblddir.bat batch file, 
set the folder path for each git branch.

e.g.
for the "development" branch,
setx OPENSAL_BUILD_DIR V:\Build\QuadSal\dev\
refreshenv

todo: find another way to keep branch builds separate.
